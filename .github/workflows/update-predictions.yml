name: Update Predictions

on:
  schedule:
    # Every hour
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4.2.2

      - name: Set up Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run AFL TELO model (predictions.json)
        run: python telo.py --year 2026

      - name: Run TELO predictions engine (predictions_sports.json)
        run: python generate.py

      - name: Commit updated predictions
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/predictions.json data/player_ratings.json data/predictions_sports.json data/ratings/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update predictions [$(date -u '+%Y-%m-%d %H:%M UTC')]"
            git pull --rebase origin main
            git push
          fi
